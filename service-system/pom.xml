<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <!-- 继承微服务父工程 -->
    <parent>
        <groupId>com.jwy</groupId>
        <artifactId>springcloud-demo</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <!-- 默认 packaging=jar：执行 package 会产出可执行 fat jar -->
    <artifactId>service-system</artifactId>
    <name>service-system</name>
    <description>service-system</description>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <!-- MyBatis-Plus（Spring Boot 4 专用启动器，兼容 Spring Framework 7 / Boot 4.0.8） -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
            <version>3.5.16</version>
        </dependency>
        <!-- MySQL 数据库驱动：连接真实 MySQL，driver 由 application.yml 指定 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- Web 模块：提供 @RestController / Spring MVC 注解，用于暴露 HTTP 接口 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!-- 依赖本模块的对外契约（DTO + Feign 接口） -->
        <dependency>
            <groupId>com.jwy</groupId>
            <artifactId>service-system-api</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- SpringDoc OpenAPI：自动生成 OpenAPI 3 文档并内置 Swagger UI。
             暴露 /v3/api-docs（JSON，可导入 Apifox / Postman）与 /swagger-ui.html（可视化界面）。
             需 Spring Boot 4 兼容版本（3.x 线）；注解类由 service-system-api 传递提供。 -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>3.1.1</version>
        </dependency>
        <!-- Nacos 服务注册与发现：本服务启动后自动注册到 Nacos，供网关通过 lb://service-system 发现 -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
        <!-- 测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- 启用 Spring Boot repackage，产出可执行 jar -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <!-- 继承父工程 pluginManagement 中预置的 lombok 注解处理器路径 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
