<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <!-- 继承微服务父工程 -->
    <parent>
        <groupId>com.jwy</groupId>
        <artifactId>springcloud-demo</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>service-auth-api</artifactId>
    <name>service-auth-api</name>
    <description>service-auth 对外服务契约（DTO + Feign 客户端接口），供其他微服务依赖调用</description>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <!-- Spring MVC 注解（@RequestMapping / @PostMapping 等），Feign 与 Controller 共用同一套 HTTP 契约 -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
        </dependency>
        <!-- OpenFeign：提供 @FeignClient 注解，定义声明式远程调用客户端。
             设为 optional：api 模块自身用它编译接口；但实现模块（service-auth）依赖本模块时
             不会传递引入 openfeign / spring-cloud，避免触发与 Spring Boot 4 不兼容的 cloud 自动配置。
             真正要用 Feign 调用的消费方服务，需自行声明 spring-cloud-starter-openfeign 并加 @EnableFeignClients。

             ⚠️ 这里刻意不写 <version>：版本由父工程 import 的 spring-cloud-dependencies BOM 统一管理
             （2025.1.0 → openfeign 5.0.0）。
             历史教训：此处曾硬编码 <version>4.1.0</version>，那是 Spring Cloud 2023.0.x 的版本线；
             虽然因 optional 不向下传递而暂时没暴露问题，但消费方拿到的却是 BOM 管理的 5.0.x，
             契约接口与运行期 client 版本线错配，属于本项目踩过的同类坑（HttpRedirects、
             WebServerInitializedEvent 均因版本线错配导致）。 -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
            <optional>true</optional>
        </dependency>
        <!-- SpringDoc OpenAPI 注解（io.swagger.v3.oas.annotations.*）：仅提供注解类，
             不引入 servlet / UI，保证 api 契约模块轻量。
             实现模块再加 springdoc-openapi-starter-webmvc-ui 才会真正生成并暴露文档。
             注意：必须 3.x 线，Boot 3 线的 2.8.x 与 Spring Boot 4 不兼容。 -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-api</artifactId>
            <version>3.1.1</version>
        </dependency>
    </dependencies>

</project>
