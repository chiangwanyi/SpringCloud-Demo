<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <!-- 继承微服务父工程 -->
    <parent>
        <groupId>com.jwy</groupId>
        <artifactId>springcloud-demo</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>service-id-gen</artifactId>
    <name>service-id-gen</name>
    <description>
        service-id-gen 分布式发号服务（Leaf-segment 号段模式）。
        自实现双 buffer 号段发号：对外提供 /api/id/segment（领号段）与
        /api/id/next（领单个 ID，服务端持段）两个接口。
    </description>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- MyBatis-Plus（Spring Boot 4 专用启动器，兼容 Spring Framework 7 / Boot 4.0.8）。
             这里只用它的 DataSource 自动配置与依赖装配能力，号段表本身用原生 @Update/@Select 操作。 -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
            <version>3.5.16</version>
        </dependency>

        <!-- MySQL 数据库驱动 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- 本模块实现的契约（IdGenApi + SegmentDTO + 号段消费组件） -->
        <dependency>
            <groupId>com.jwy</groupId>
            <artifactId>service-id-gen-api</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Nacos 服务注册与发现：发号服务必须注册，业务才能用 lb://service-id-gen 找到它。
             ★ 本模块刻意【不引入】openfeign，也【不加】@EnableFeignClients：
             它是 provider（实现 IdGenApi），若同时引入 Feign 会为 IdGenApi 生成代理 Bean，
             与自己实现该接口的 IdGenController 类型相同，启动直接
             NoUniqueBeanDefinitionException。这一点与 service-system 完全一致。 -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>

        <!-- SpringDoc OpenAPI：自动生成 OpenAPI 3 文档并内置 Swagger UI（3.x 线才兼容 Boot 4） -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>3.1.1</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
