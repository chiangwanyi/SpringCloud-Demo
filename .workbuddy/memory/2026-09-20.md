# 2026-09-20

- 将微服务数据库从 H2 内存库切换为真实 MySQL：`service-system` / `service-auth` 两模块的 pom.xml 把 `com.h2database:h2` 换成 `com.mysql:mysql-connector-j`，application.yml 的 datasource 改为 `com.mysql.cj.jdbc.Driver` + `jdbc:mysql://rxs:3306/<service_x>`（连接信息见 `mysql.md`）。现有 `schema.sql` 已兼容 MySQL。

- 数据库初始化与 CRUD：新增 `sql/create_databases.sql`（分别 CREATE DATABASE service_system / service_auth，utf8mb4）。两个模块的 `schema.sql` 追加默认演示数据（部门/角色/用户/用户角色；认证账号 admin/zhangsan，密码明文仅演示）。跨库检查结论：两服务只访问各自表（service-system 仅 sys_user 系、service-auth 仅 auth_account），无任何对方表 Mapper/SQL，也未启用 @EnableFeignClients 跨服务直连——无“偷偷连对方库”。为两服务补齐 CRUD：service-system 的 SysUserApi/Controller/Service 增加 POST/PUT/DELETE；service-auth 的 AuthApi/Controller/Service 增加账号 CRUD（/api/auth/account*）。新增 DTO：SysUserSaveDTO、AuthAccountDTO、AuthAccountSaveDTO。四个模块离线 `mvn compile` 通过。

- 接入 Nacos 服务注册与发现（局域网 Nacos 见 `nacos.md`，控制台 rxs:8080、客户端端口 8848、账号 nacos/123456，已开认证）。父 POM `dependencyManagement` import 两个 BOM：`spring-cloud-alibaba-dependencies:2025.0.0.0` 与 `spring-cloud-dependencies:2025.0.3`（**此版本线 4.3.x 错误，见下方修正**）。`service-system`/`service-auth`/`service-gateway` 三模块加 `spring-cloud-starter-alibaba-nacos-discovery`，网关额外加 `spring-cloud-starter-loadbalancer`；各模块 `application.yml` 配 `spring.cloud.nacos.discovery.server-addr: rxs:8848` + 认证账号，启动类加 `@EnableDiscoveryClient`；网关路由 uri 由写死 localhost 改为 `lb://service-system`、`lb://service-auth`。`mvn -B -DskipTests compile` 全模块 BUILD SUCCESS（nacos-discovery/loadbalancer 经代理从 Maven Central 下载成功）。未接入 Nacos Config 配置中心。

- 【修正】修复三服务启动失败：根因是父 POM 的 Spring Cloud BOM 版本线错配——`2025.0.0.0`/`2025.0.3` 属于 4.3.x 线（编译于 Boot 4.0 里程碑），与 Boot 4.0.8 GA 不兼容，导致 `NoClassDefFoundError`（`HttpRedirects` 网关、`WebServerInitializedEvent` service 模块）。已将两个 BOM 改为 `spring-cloud-alibaba-dependencies:2025.1.0.0` + `spring-cloud-dependencies:2025.1.0`（5.0.x 线，兼容 Boot 4.0.8）。`dependency:tree` 确认网关不再拉 `spring-cloud-gateway-server-mvc`、spring-cloud-* 统一到 5.0.x；`mvn -B -DskipTests compile` 全 6 模块 BUILD SUCCESS。详见 MEMORY.md「版本线铁律」。

- 接入 Nacos 配置中心（Config），将 `spring.datasource`（jdbc:mysql url + 账号密码）从本地 application.yml 外置到 Nacos，避免硬编码连接信息：
  - 经 Nacos 3.x API 发布配置：登录 `POST /nacos/v3/auth/user/login` 取 accessToken；发布 `POST /nacos/v3/admin/cs/config?dataId=service-system.yaml&groupName=DEFAULT_GROUP&type=yaml`（旧 `/v1/cs/configs` 在 v3.x 已 404）。dataId `service-system.yaml`/`service-auth.yaml` 均已发布并验证可读。
  - 两服务 pom 新增 `com.alibaba.cloud:spring-cloud-starter-alibaba-nacos-config`；application.yml 用 `spring.cloud.nacos.config`（server-addr/file-extension=yaml/group）+ `spring.config.import: nacos:<服务名>.yaml` 引入，删除本地 `spring.datasource` 块。
  - 编译验证：`mvn -B -DskipTests -pl service-system,service-auth -am compile` BUILD SUCCESS（系统 Maven 在 /e/Env/apache-maven-3.9.16，需 `JAVA_HOME=E:/Env/jdk-21.0.12.1`；`./mvnw` 因沙箱缺 uname/dirname 不可用）。
  - 运行时待确认：启动服务应能从 Nacos 拉到 datasource 并连 MySQL（rxs:3306 需可达）。
